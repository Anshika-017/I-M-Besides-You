# Segmentation (src/procmine/segment.py, evaluate.py) uses only the Python
# standard library, deliberately -- see WORKLOG.md. Labeling
# (src/procmine/features.py, label.py) uses scikit-learn for TF-IDF text
# vectorization and agglomerative clustering. pytest runs the unit tests.
pytest>=7.0
scikit-learn>=1.3
scipy>=1.10
numpy>=1.24

# Step 3 prototype (automation/): Flask serves the mock reproduction of
# the observed queue UI; Playwright drives it. After `pip install -r
# requirements.txt`, also run `python3 -m playwright install chromium`
# once (downloads the browser binary; not a pip package).
flask>=3.0
playwright>=1.40
